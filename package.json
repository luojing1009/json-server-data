{
  "name": "json-server-data",
  "version": "1.0.0",
  "description": "基于json-server的RESTful API接口服务",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3000",
    "dev": "json-server --watch db.json --port 3000 --host 0.0.0.0",
    "build": "echo 'Build completed'",
    "test": "echo 'No tests specified'"
  },
  "keywords": [
    "json-server",
    "api",
    "rest",
    "mock"
  ],
  "author": "你的名字",
  "license": "MIT",
  "dependencies": {
    "json-server": "^0.17.4",
    "serverless-http": "^3.2.0"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
